import { NextRequest, NextResponse } from 'next/server';
import { RequestAdapter, ResponseAdapter } from './types';
export declare class NextRequestAdapter implements RequestAdapter {
    private req;
    constructor(request: NextRequest);
    setRequestCookie(name: string, value: string): void;
    getHeader(name: string): string | null;
}
export declare class NextResponseAdapter implements ResponseAdapter {
    private res;
    constructor(response: NextResponse);
    getHeader(name: string): string | null;
    setHeader(name: string, value: string): this;
}
//# sourceMappingURL=NextMiddlewareAdapter.d.ts.map